import 'package:flutter/cupertino.dart';

/// Allows the user to select among a list of choices.
///
class SelectView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("hello selector");
  }
}
